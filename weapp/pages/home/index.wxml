<!----------   wxs start ----------->
<wxs module="m1">
"use strict";

var getTime = function getTime(time) {
  var date = getDate(time);
  var hour = date.getHours();
  var mins = date.getMinutes();
  var sec = date.getSeconds();
  var milli = date.getMilliseconds();
  return "".concat(hour, ":").concat(mins, ":").concat(sec, ".").concat(milli);
};

module.exports.getTime = getTime;
</wxs>
<!----------   wxs end   ----------->

<view class="homes">
  <van-search bind_init="__initComponent" shape="round" focus value="{{ value }}" placeholder="请输入搜索关键词"></van-search>
  <van-tabs bind_init="__initComponent" active="{{ active }}">
    <van-tab bind_init="__initComponent" title="推荐">
      <biaoqianA bind_init="__initComponent"></biaoqianA>
    </van-tab>
    <van-tab bind_init="__initComponent" title="功能2">
      <biaoqianB bind_init="__initComponent"></biaoqianB>
    </van-tab>
    <van-tab bind_init="__initComponent" title="功能3">
      <biaoqianC bind_init="__initComponent"></biaoqianC>
    </van-tab>
    <van-tab bind_init="__initComponent" title="功能4">
      <biaoqianD bind_init="__initComponent"></biaoqianD>
    </van-tab>
    
  </van-tabs>
</view>
